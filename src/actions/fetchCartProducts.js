import { getCartProducts } from "selectors";

const getCartProducts = () => getState => getCartProducts(getState);

fetchProducts.toString = fetchProductsAsync.toString;

export default fetchProducts;
